
  # Design latent space graph

  This is a code bundle for Design latent space graph. The original project is available at https://www.figma.com/design/kgN4CFZU9VI56tkuuozm0a/Design-latent-space-graph.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  